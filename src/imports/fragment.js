import Vue from 'vue'
// add fragment imports
// this is basically so we can have divs that are listed as fragments so they won't add to our markup
import Fragment from 'vue-fragment'
Vue.use(Fragment.Plugin)